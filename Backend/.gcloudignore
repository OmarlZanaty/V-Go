# Keep the Cloud Build upload small and never ship secrets into the image.
**/bin/
**/obj/
**/.vs/
.git/
.gitignore

# Secrets are provided at runtime via env vars / Secret Manager, not baked in.
**/appdata/secrets/
**/appsettings.*.json
*.env
